---
name: build-enterprise-ai-prd
description: 从零创建、续写或重构企业级 AI 产品 PRD、方案决策、项目复盘与面试答辩长文，产出与“AI 合同审查 PRD 方案决策与项目答辩完整版”同等深度的可落地文档。适用于用户要求写完整 AI PRD/项目方案/作品集/项目复盘/合同审查方案，补齐 Why 与 Why not、功能级 PRD、规则+RAG+LLM 全链路、数据知识治理、模型选型、Prompt/Schema、异常降级人工恢复、安全评测、Bad Case、运营归因和面试追问，或把已有短文扩成零基础学员可学习和迁移的企业实战模板。默认以文件为外部记忆分阶段生成，避免一次性长文造成上下文爆炸；也支持审计和批量修订已有 Markdown 文档。
---

# 企业级 AI PRD 全案生成

## 目标

产出一份同时可用于：

- 产品、算法、研发、法务/业务、安全和运营评审；
- PoC、MVP、灰度、验收和上线治理；
- 零基础学员理解全流程、独立迁移项目；
- AI 产品经理面试项目答辩。

不要把目录填满当成完成。最终文档必须形成：

`业务问题 → 用户任务 → AI 必要性 → MVP 取舍 → 产品/AI/数据/系统方案 → 风险控制 → 评测 Gate → 上线运营 → 决策与面试证据`

## 不可妥协的原则

1. **先业务，后 AI**：未明确用户、任务、现状、损失、规则和边界前，不进入模型比较。
2. **每项设计解释 Why / Why not**：说明目标、约束、依据、替代方案、代价、失败条件和重评条件。
3. **事实诚实**：严格区分 `已确认事实 / 有来源推断 / 待验证假设 / 建议方案 / 待决策事项`。不得编造访谈、上线指标、模型胜出、企业规则或个人职责。
4. **从输入到结果串联**：不能把规则、RAG、LLM、Prompt、校验、人工和写回写成彼此独立的名词章节。
5. **完整设计失败**：关键流程必须有正常、异常、降级、人工、恢复和防复发。
6. **可执行而非套话**：功能写到触发、输入、处理、输出、状态、权限、异常、恢复、埋点和验收；评测写到数据、Gold、指标、阈值依据、Gate 和未通过动作。
7. **教学而非代写答案**：告诉学员最该看什么、怎么推导、哪些可迁移、哪些必须重做，并给独立练习。
8. **图必须表达关系**：架构、数据流、调用时序、状态和治理闭环用 Mermaid/图示，不只用文字列表。
9. **学员必须本人手画**：生成的图只作结构参考；文档显著提醒学员关闭示例后自己手画，AI 只用于查漏。
10. **原型边界明确**：没有真实原型时明确写“暂不提供图片”；企业落地必须补可点击原型、状态/权限说明和可用性走查。

详细质量标准见 [references/quality-contract.md](references/quality-contract.md)。

## 选择工作模式

| 模式 | 何时使用 | 用户确认策略 |
| --- | --- | --- |
| `guided`（默认） | 从零生成或业务信息不完整 | 只在 4 个里程碑确认，不逐章打断 |
| `autopilot` | 用户要求直接完成且允许先用假设版 | 仅遇到会改变场景、责任、安全或事实真实性的阻塞项时询问；其余标假设继续 |
| `audit` | 已有长文，需要深化/补缺 | 先给问题地图与修改批次，再分批改文件 |
| `pack-only` | 内容已确认，只需整理成完整版 | 仍在内部按批次处理和校验；不得在一次聊天回复中生成全文 |

若用户没有指定，使用 `guided`。即使用户要求“一次完成”，也要**内部按批次写文件**，最后只在对话中汇报路径、决策和待确认项。

## 长文上下文控制

把文件当作外部记忆，不依赖聊天历史。

### 永久小上下文

每次只固定读取：

1. `project-brief.md`：项目、用户、场景、阶段、目标、非目标；
2. `evidence-ledger.md`：事实、来源、假设、缺口；
3. `decision-log.md`：已冻结决策、Why/Why not、重评条件；
4. `context-snapshot.md`：不超过约 1500 中文字的最新状态；
5. 当前批次章节和当前批次所需 reference。

不要每轮重新加载完整成稿。先把当前 `SKILL.md` 所在目录记为 `$SKILL_DIR`，使用 `$SKILL_DIR/scripts/extract_prd_section.py` 只读取当前章节。

### 批次划分

| 批次 | 章节 | 主要决策 |
| --- | --- | --- |
| B0 | 0—1 | 文档契约、一页纸项目结论 |
| B1 | 2—5 | 业务证据、用户、AI 必要性、目标与 MVP |
| B2 | 6—8 | 体验、功能级 PRD、AI 任务边界 |
| B3 | 9—12 | 数据/RAG、模型、Prompt/Agent、系统架构 |
| B4 | 13—16 | 异常、降级/人工/恢复、安全、评测 Gate |
| B5 | 17—19 | 归因、Bad Case、运营迭代 |
| B6 | 20—22 + 附录 | 计划、Decision Log、面试答辩和证据索引 |

每批完成后更新状态文件，只记录决策和接口，不复制长篇正文。

## 开始前：建立生产契约

先读取 [references/quality-contract.md](references/quality-contract.md)，再读取用户指定资料、链接笔记和现有文档。创建或审计完整目录时读取一次 [references/chapter-contract.md](references/chapter-contract.md)，之后只按当前批次回读相关段落。输出一页生产契约：

| 项 | 必须确认 |
| --- | --- |
| 项目性质 | 真实项目 / 课程项目 / 模拟项目 |
| 当前阶段 | 调研 / PoC / MVP / 灰度 / 上线 |
| 核心用户与一期场景 | 一个可闭环的用户任务，不是宽泛行业 |
| 读者和用途 | 学员、评审、研发、面试或组合 |
| 已有证据 | 访谈、流程、数据、规则、系统、样本、结果 |
| 禁止声称 | 无证据的公司、指标、部署、模型结果和本人职责 |
| 按需能力 | Rule / RAG / Agent / Tool / 多模态；不用则写 N/A 与原因 |
| 输出位置与模式 | 最终 Markdown 路径、guided/autopilot/audit |

只询问会阻塞核心决策的 1—3 个问题。其余信息进入待验证假设，不要连续追问把生产卡死。

### Guided 的四个确认 Gate

1. **Gate 0 生产契约**：确认项目事实、一期场景、证据等级和产出路径。
2. **Gate A 策略**（B1 后）：确认问题、AI 必要性、MVP 和非目标。
3. **Gate B 方案**（B3 后）：确认功能、Rule/RAG/LLM/Tool 边界、架构和人工责任。
4. **Gate C 发布**（B5 后）：确认红线、评测、降级、运营和是否允许形成最终答辩版。

不要每个小节都让用户确认。若用户未及时回复且允许继续，保留 `待确认` 标记并向后推进不依赖该决策的章节。

## 初始化项目

默认使用 bundled 母版，不要从空白 Markdown 开始：

```bash
python3 "$SKILL_DIR/scripts/init_prd_project.py" \
  "项目名称" \
  --output-dir "目标目录" \
  --mode guided \
  --project-type course
```

脚本会创建最终骨架和隐藏工作区，不覆盖已有文件。母版位于 `assets/ai-prd-master-template.md`。

处理已有文档时不初始化新文档；先复制备份或确认目标为可更新文件，再用 `audit` 模式。

完整章节职责和必需产物见 [references/chapter-contract.md](references/chapter-contract.md)。不要一次加载母版和全部 references；按批次读取。

## 每个批次的执行循环

### 1. 对齐

- 读取永久小上下文、当前章节、前一批输出接口和后一批输入要求。
- 列出本批要做的决策、已有证据、未知项和不得越界的上层决策。
- 发现上层决策冲突时，先登记变更请求，不能在下游章节静默改口径。

### 2. 设计

每个实质章节回答七问：

1. Why：解决什么决策或风险？
2. Why not：为何不做、延后、人工、规则、搜索或更简单方案？
3. Know how：角色、触发、输入、处理、输出、状态、权限、指标。
4. Evidence：依据来自哪里，证据等级是什么？
5. Boundary：异常、Corner Case、降级、人工和恢复。
6. Migration：方法可复用什么，业务变量必须重做什么？
7. Interview：主问、约束追问、考察点和答题主线。

### 3. 写入

- 直接写目标 Markdown 文件，不在聊天中粘贴数万字。
- 保留已有 YAML、编号、Wiki-link 和 Dataview。
- 使用相对路径处理 Vault 文件。
- 用表格承载决策和字段，用自然语言解释因果，不堆技术名词。
- 用 Mermaid 表达架构/流程/时序；在图旁添加本人手画提醒。
- 交互无图时添加“暂不提供任何图片；企业落地必须补原型”的显著提示。

### 4. 批次自检

- 每个设计是否有目标、依据、替代和失败处理？
- 功能是否能交给研发/测试，而非“系统智能分析”？
- 上游对象、版本、权限能否贯穿到结果和日志？
- 是否把假设写成事实或把目标写成结果？
- 是否有至少一个正常 Case、一个异常 Case、一个 Corner Case？
- 是否告诉学员如何阅读、迁移和应对面试？
- 是否与已冻结决策矛盾？

### 5. 压缩状态

更新 `context-snapshot.md`，只保留：

- 本批新增/改变的决策；
- 新增对象和接口；
- 指标与口径；
- 未决问题；
- 对后续批次的输入；
- 需全局回查的矛盾。

## 高复杂度章节加载规则

- 写 B2/B3 前读取 [references/rule-rag-llm-schema.md](references/rule-rag-llm-schema.md)。
- 写 B4/B5 前读取 [references/resilience-eval-operations.md](references/resilience-eval-operations.md)。
- 为零基础学员添加阅读提醒、迁移练习和面试卡时读取 [references/teaching-interview.md](references/teaching-interview.md)。
- 只需普通非 RAG 项目时，不加载 RAG 细节；明确哪些模块为 N/A。

## 必须达到的深度

### 功能 PRD

每个 P0 功能至少写：功能 ID、用户/任务、目标与非目标、触发/前置、输入与来源、主流程、业务规则、AI/规则/人工边界、输出、状态机、权限、异常/降级/恢复、交互提示、埋点、验收 Case 和依赖。

### Rule + RAG + LLM

必须同时画离线知识链路和在线请求链路，并串起输入、解析、存储、规则、案例 Embedding、检索/Rerank、Evidence Package、Prompt/LLM、Schema/业务校验、人工、工作流、日志和评测。Prompt 不能替代权限、规则、事实和事务一致性。

### 评测与治理

必须从任务卡和风险出发，写数据来源/分布、Gold、组件评测、端到端、安全、故障、成本容量、Gate、灰度、回滚和线上复评。第 13—19 章不能只列问题名，必须给检测、止损、定位、修复、回归、恢复和 Case。

## 最终集成与验证

完成 B6 后从结果反向检查：

1. 面试结论能否追溯到 Decision Log、评测或项目事实？
2. 评测指标能否追溯到任务、风险和日志字段？
3. 每个 P0 需求能否追溯到用户问题、功能、AI/规则任务和验收 Case？
4. 每个外部动作是否有权限、确认、幂等、对账和审计？
5. 每个上线能力是否有降级、人工、恢复和 owner？
6. 架构图中的每条箭头是否能在正文找到输入/输出契约？
7. 是否处理了已影响的历史对象，而不只修新版本？

执行：

```bash
python3 "$SKILL_DIR/scripts/validate_prd.py" "最终文档.md"
python3 "$SKILL_DIR/scripts/validate_prd.py" "最终文档.md" --strict
```

先修结构错误，再审查警告。若环境可用 Mermaid parser，再解析所有 Mermaid block；不要只凭肉眼判断。

## 完成标准

只有同时满足以下条件才宣告完成：

- 0—22 章和附录存在，按适用性完成或说明 N/A；
- 事实、假设、建议和目标未混淆；
- 业务、功能、AI、数据、系统、评测、治理和面试证据闭环；
- 架构和关键流程以图展示，并含本人手画提醒；
- 无图的交互部分明确原型缺口；
- 关键 Case 可重放、可归因、可回归、可恢复；
- 没有用“优化 Prompt、转人工、加强监控”等套话代替方案；
- 严格校验没有结构错误，剩余警告有明确理由；
- 最终回复只汇报产物链接、已冻结决策、待确认项和验证结果。
